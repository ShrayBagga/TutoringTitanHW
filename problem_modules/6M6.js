// --- Helper Functions ---
function getRandomInt(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }
function formatAnswer(val, p=2) { return parseFloat(val.toFixed(p)).toString().replace(/\.00$/, ''); }
function gcd(a, b) { return b === 0 ? a : gcd(b, a % b); }

const problemGenerators = [
    // --- Original 20 Problems ---
    () => { const l = getRandomInt(5, 12); const w = getRandomInt(3, 8); return { problem: `Find the area of a rectangle with length \\(${l}\\) cm and width \\(${w}\\) cm.`, answer: `\\(${l*w}\\) cm\\(^{2}\\)`, checkAnswer: (l*w).toString() }; },
    () => { const s = getRandomInt(4, 10); return { problem: `Find the area of a square with a side length of \\(${s}\\) inches.`, answer: `\\(${s*s}\\) in\\(^{2}\\)`, checkAnswer: (s*s).toString() }; },
    () => { const b = getRandomInt(6, 14); const h = getRandomInt(4, 9); return { problem: `Find the area of a triangle with a base of \\(${b}\\) m and a height of \\(${h}\\) m.`, answer: `\\(${0.5*b*h}\\) m\\(^{2}\\)`, checkAnswer: (0.5*b*h).toString() }; },
    () => { const b = getRandomInt(5, 10); const h = getRandomInt(4, 8); return { problem: `Find the area of a parallelogram with a base of \\(${b}\\) ft and a height of \\(${h}\\) ft.`, answer: `\\(${b*h}\\) ft\\(^{2}\\)`, checkAnswer: (b*h).toString() }; },
    () => { const b1 = getRandomInt(8, 12); const b2 = getRandomInt(4, 7); const h = getRandomInt(5, 8); return { problem: `Find the area of a trapezoid with bases \\(${b1}\\) and \\(${b2}\\) and height \\(${h}\\).`, answer: `\\(${formatAnswer(0.5*(b1+b2)*h)}\\)`, checkAnswer: (0.5*(b1+b2)*h).toString() }; },
    () => { const l=getRandomInt(3,6); const w=getRandomInt(3,6); const h=getRandomInt(3,6); return { problem: `Find the volume of a rectangular prism with length \\(${l}\\), width \\(${w}\\), and height \\(${h}\\).`, answer: `\\(${l*w*h}\\) cubic units`, checkAnswer: (l*w*h).toString() }; },
    () => { const s = getRandomInt(3, 7); return { problem: `Find the volume of a cube with side length \\(${s}\\) cm.`, answer: `\\(${s*s*s}\\) cm\\(^{3}\\)`, checkAnswer: (s*s*s).toString() }; },
    () => { const l=getRandomInt(2,5); const w=getRandomInt(2,5); const h=getRandomInt(2,5); return { problem: `Find the surface area of a rectangular prism with dimensions \\(${l} \\times ${w} \\times ${h}\\).`, answer: `\\(${2*(l*w+l*h+w*h)}\\) square units`, checkAnswer: (2*(l*w+l*h+w*h)).toString() }; },
    () => { const s = getRandomInt(2, 6); return { problem: `Find the surface area of a cube with side length \\(${s}\\).`, answer: `\\(${6*s*s}\\) square units`, checkAnswer: (6*s*s).toString() }; },
    () => { const b=getRandomInt(3,5); const h=getRandomInt(4,6); const l=getRandomInt(5,8); const hyp = Math.sqrt(b*b+h*h); return { problem: `Find the surface area of a right triangular prism with base legs \\(${b}\\) and \\(${h}\\) and length \\(${l}\\).`, answer: `\\(${formatAnswer(b*h + (b+h+hyp)*l)}\\)`, checkAnswer: formatAnswer(b*h + (b+h+hyp)*l) }; },
    () => { const x1=getRandomInt(1,3), y1=getRandomInt(1,3), x2=getRandomInt(4,6), y2=y1, x3=x1, y3=getRandomInt(4,6); return { problem: `A right triangle is plotted at (\\(${x1},${y1}\\)), (\\(${x2},${y2}\\)), and (\\(${x3},${y3}\\)). Find its area.`, answer: `\\(${0.5*(x2-x1)*(y3-y1)}\\) sq. units`, checkAnswer: (0.5*(x2-x1)*(y3-y1)).toString() }; },
    () => { const l1=getRandomInt(8,12), w1=getRandomInt(6,10), l2=getRandomInt(3,5), w2=getRandomInt(3,5); return { problem: `A large rectangle (\\(${l1} \\times ${w1}\\)) has a smaller rectangle (\\(${l2} \\times ${w2}\\)) cut out. Find the area of the remaining shape.`, answer: `\\(${l1*w1 - l2*w2}\\) sq. units`, checkAnswer: (l1*w1 - l2*w2).toString() }; },
    () => { const side = getRandomInt(4, 7); return { problem: `A net of a cube is laid out. If one square face has an area of \\(${side*side}\\) sq. cm, what is the total surface area of the cube?`, answer: `\\(${6*side*side}\\) cm\\(^{2}\\)`, checkAnswer: (6*side*side).toString() }; },
    () => { const num=getRandomInt(3,5), den=getRandomInt(num+1,8), l=getRandomInt(4,10); return { problem: `What is the volume of a rectangular prism with dimensions \\(\\frac{${num}}{${den}}\\) ft, \\(${l}\\) ft, and \\(${den}\\) ft?`, answer: `\\(${num*l}\\) ft\\(^{3}\\)`, checkAnswer: (num*l).toString() }; },
    () => { const area = [25, 36, 49, 64, 81][getRandomInt(0,4)]; return { problem: `The area of a square is \\(${area}\\) m\\(^{2}\\). What is its side length?`, answer: `\\(${Math.sqrt(area)}\\) m`, checkAnswer: Math.sqrt(area).toString() }; },
    () => { const volume = [27, 64, 125][getRandomInt(0,2)]; return { problem: `The volume of a cube is \\(${volume}\\) cm\\(^{3}\\). What is its side length?`, answer: `\\(${Math.cbrt(volume)}\\) cm`, checkAnswer: Math.cbrt(volume).toString() }; },
    () => { const b = 10; const h = 5; const area = 0.5 * b * h; return { problem: `A triangular garden has a base of \\(${b}\\) feet and an area of \\(${area}\\) sq. feet. What is its height?`, answer: `\\(${h}\\) feet`, checkAnswer: h.toString() }; },
    () => { const l = 5; const w = 4; const area = l * w; return { problem: `A rectangular room has an area of \\(${area}\\) sq. meters and a length of \\(${l}\\) meters. What is its width?`, answer: `\\(${w}\\) meters`, checkAnswer: w.toString() }; },
    () => { const l=getRandomInt(5,8), w=getRandomInt(5,8), h=getRandomInt(5,8); return { problem: `How many 1x1x1 unit cubes can fit inside a rectangular prism of size \\(${l} \\times ${w} \\times ${h}\\)?`, answer: `\\(${l*w*h}\\)`, checkAnswer: (l*w*h).toString() }; },
    () => { const l=getRandomInt(8,10), w=getRandomInt(4,6), h=getRandomInt(2,4); return { problem: `A swimming pool is \\(${l}\\)m long, \\(${w}\\)m wide, and \\(${h}\\)m deep. How much water can it hold in cubic meters?`, answer: `\\(${l*w*h}\\) m\\(^{3}\\)`, checkAnswer: (l*w*h).toString() }; },

    // --- New 20 Problems ---
    () => { const l = getRandomInt(5, 10) + 0.5; const w = getRandomInt(2, 8); return { problem: `A garden bed is \\(${l}\\) feet long and \\(${w}\\) feet wide. What is its area?`, answer: `\\(${formatAnswer(l*w)}\\) ft\\(^{2}\\)`, checkAnswer: formatAnswer(l*w) }; },
    () => { const num = getRandomInt(3, 7); const den = getRandomInt(num+1, 9); const s = num/den; return { problem: `Find the volume of a cube with a side length of \\(\\frac{${num}}{${den}}\\) inches.`, answer: `\\(\\frac{${num**3}}{${den**3}}\\) in\\(^{3}\\)`, checkAnswer: `${num**3}/${den**3}` }; },
    () => { const b = 12; const h = getRandomInt(4, 7); const area = 0.5*b*h; return { problem: `A kite is shaped like a triangle with a base of \\(${b}\\) inches and an area of \\(${area}\\) square inches. What is the height of the kite?`, answer: `\\(${h}\\) inches`, checkAnswer: h.toString() }; },
    () => { const l = 10, w = 8, h = 6; const sa = 2*(l*w + l*h + w*h); return { problem: `You want to paint a wooden box with dimensions \\(${l}\" \\times ${w}\" \\times ${h}\"\\). What is the total surface area you need to cover?`, answer: `\\(${sa}\\) in\\(^{2}\\)`, checkAnswer: sa.toString() }; },
    () => { const rect_l = 8, rect_w = 10; const tri_b = 8, tri_h = 5; const total_area = (rect_l*rect_w) + (0.5*tri_b*tri_h); return { problem: `A shape looks like a house, formed by a \\(${rect_l} \\times ${rect_w}\\) rectangle and a triangle on top with a base of \\(${tri_b}\\) and height of \\(${tri_h}\\). What is the total area?`, answer: `\\(${total_area}\\) sq. units`, checkAnswer: total_area.toString() }; },
    () => { const l = 2.5, w = 3.5, h = 4; const volume = l*w*h; return { problem: `Find the volume of a rectangular prism with dimensions \\(${l}\\)m, \\(${w}\\)m, and \\(${h}\\)m.`, answer: `\\(${formatAnswer(volume)}\\) m\\(^{3}\\)`, checkAnswer: formatAnswer(volume) }; },
    () => { const s = 1.5; const sa = 6*s*s; return { problem: `What is the surface area of a cube with a side length of \\(${s}\\) cm?`, answer: `\\(${formatAnswer(sa)}\\) cm\\(^{2}\\)`, checkAnswer: formatAnswer(sa) }; },
    () => { const l = 9, w = 7; const perimeter = 2*(l+w); return { problem: `A rectangular field has a length of \\(${l}\\) yards and a width of \\(${w}\\) yards. How much fencing is needed to go around it?`, answer: `\\(${perimeter}\\) yards`, checkAnswer: perimeter.toString() }; },
    () => { const x1=0, y1=0; const x2=getRandomInt(4,7); const y2=0; const x3=x2; const y3=getRandomInt(3,5); const x4=0; const y4=y3; const area = x2*y3; return { problem: `A polygon has vertices at (\\(${x1},${y1}\\)), (\\(${x2},${y2}\\)), (\\(${x3},${y3}\\)), and (\\(${x4},${y4}\\)). Find the area of the polygon.`, answer: `\\(${area}\\) sq. units`, checkAnswer: area.toString(), hint: "Plot the points. You will see it forms a rectangle." }; },
    () => { const room_l = 12, room_w = 10; const tile_s = 1; const num_tiles = (room_l*room_w)/(tile_s*tile_s); return { problem: `How many square tiles with a side length of \\(${tile_s}\\) ft are needed to cover a floor that is \\(${room_l}\\) ft long and \\(${room_w}\\) ft wide?`, answer: `\\(${num_tiles}\\) tiles`, checkAnswer: num_tiles.toString() }; },
    () => { const b = getRandomInt(3,5), h = getRandomInt(4,6), l = 10; const volume = 0.5*b*h*l; return { problem: `A wedge of cheese is a triangular prism. The triangular end has a base of \\(${b}\\) cm and height of \\(${h}\\) cm. The wedge is \\(${l}\\) cm long. What is its volume?`, answer: `\\(${volume}\\) cm\\(^{3}\\)`, checkAnswer: volume.toString() }; },
    () => { const outer_s = 10; const inner_s = 6; const area = outer_s**2 - inner_s**2; return { problem: `A square picture frame has outer dimensions of \\(${outer_s} \\times ${outer_s}\\) inches and an inner space for a picture of \\(${inner_s} \\times ${inner_s}\\) inches. What is the area of the frame itself?`, answer: `\\(${area}\\) in\\(^{2}\\)`, checkAnswer: area.toString() }; },
    () => { const volume = 120; const l = 8; const w = 5; const h = volume/(l*w); return { problem: `A rectangular container has a volume of \\(${volume}\\) cubic feet. Its length is \\(${l}\\) ft and its width is \\(${w}\\) ft. What is its height?`, answer: `\\(${h}\\) ft`, checkAnswer: h.toString() }; },
    () => { const n_faces = 6; const area_face = 10; const sa = n_faces * area_face; return { problem: `A cube is composed of \\(${n_faces}\\) identical square faces. If the area of one face is \\(${area_face}\\) cm\\(^{2}\\), what is the cube's total surface area?`, answer: `\\(${sa}\\) cm\\(^{2}\\)`, checkAnswer: sa.toString() }; },
    () => { const l = 15; const w = 10; const depth = 0.5; const volume = l * w * depth; return { problem: `A driveway that is \\(${l}\\) ft long and \\(${w}\\) ft wide is covered with concrete \\(${depth}\\) ft deep. What is the volume of the concrete used?`, answer: `\\(${volume}\\) cubic feet`, checkAnswer: volume.toString() }; },
    () => { const s1 = 3, s2 = 4, s3 = 5; const area = 0.5*s1*s2; return { problem: `A triangular park has side lengths of \\(${s1}\\) miles, \\(${s2}\\) miles, and \\(${s3}\\) miles. If this forms a right triangle, what is its area?`, answer: `\\(${area}\\) square miles`, checkAnswer: area.toString(), hint: "In a right triangle, the two shorter sides are the base and height." }; },
    () => { const r_len=5, r_wid=4, tri_base=3, tri_height=4; const area = r_len*r_wid + 0.5*tri_base*tri_height; return { problem: `A composite figure is made of a rectangle (\\(${r_len} \\times ${r_wid}\\)) and a right triangle with legs \\(${tri_base}\\) and \\(${tri_height}\\). What is the total area?`, answer: `\\(${area}\\)`, checkAnswer: area.toString() }; },
    () => { const sa = 150; const area_face = sa/6; const side = Math.sqrt(area_face); return { problem: `The surface area of a cube is \\(${sa}\\) square inches. What is the length of one side?`, answer: `\\(${side}\\) inches`, checkAnswer: side.toString() }; },
    () => { const b = 8.5, h = 10; const area = b * h; return { problem: `What is the area of a parallelogram with a base of \\(${b}\\) inches and a height of \\(${h}\\) inches?`, answer: `\\(${area}\\) in\\(^{2}\\)`, checkAnswer: area.toString() }; },
    () => { const volume = 200; const area_base = 25; const height = volume / area_base; return { problem: `A rectangular prism has a volume of \\(${volume}\\) cm\\(^{3}\\). If the area of its base is \\(${area_base}\\) cm\\(^{2}\\), what is its height?`, answer: `\\(${height}\\) cm`, checkAnswer: height.toString() }; },
];

function generate(settings) {
    const generator = problemGenerators[getRandomInt(0, problemGenerators.length - 1)];
    const problemData = generator();
    return { ...problemData, hint: problemData.hint || "Memorize the area and volume formulas for basic shapes." };
}

export const module = {
    topicId: '6M6',
    topicName: 'Geometry: Area & Volume',
    generateProblem: generate
};

